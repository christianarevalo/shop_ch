<p id="notice"><%= notice %></p>

<p>
  <strong>Is active:</strong>
  <%= @variant.is_active %>
</p>

<p>
  <strong>Price:</strong>
  <%= @variant.price %>
</p>

<p>
  <strong>Quantity:</strong>
  <%= @variant.quantity %>
</p>

<%= link_to 'Edit', edit_product_variant_path(@product, @variant) %> |
<%= link_to 'Back', product_variants_path %>
